import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Optionally configure image domains if you load external images
  images: {
    unoptimized: false,
  },
}

export default nextConfig
