# Claude Code Project Rules

## Build & Development Commands

- **Never run `npm run build` or `npm run dev` unless explicitly requested by the user.**
- Wait for user confirmation before running any build or development server commands.
